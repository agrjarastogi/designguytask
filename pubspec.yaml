name: ecommerce
description: Task for ecommerce app

version: 1.0.0+1

environment:
  sdk: ">=2.16.2 <3.0.0"

dependencies:
  flutter:
    sdk: flutter


  # The following adds the Cupertino Icons font to your application.
  # Use with the CupertinoIcons class for iOS style icons.
  cupertino_icons: ^1.0.2
  objectbox: ^1.4.1
  objectbox_flutter_libs:
  path_provider:
  dio:

dev_dependencies:
  flutter_test:
    sdk: flutter

  flutter_lints: ^1.0.0
  objectbox_generator:
  build_runner:

flutter:

  uses-material-design: true
