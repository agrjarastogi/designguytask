import 'package:flutter/material.dart';


ColorScheme colorSchemeLight = ColorScheme.fromSeed(seedColor: Colors.red);


ColorScheme colorSchemeDark = ColorScheme.fromSeed(seedColor: Colors.red, brightness: Brightness.dark);